{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "cd integration-tests && npm test",
    "build": "scripts/build",
    "bump": "scripts/bump",
    "publish": "scripts/publish"
  },
  "workspaces": [
    "client",
    "server",
    "protocol",
    "integration-tests"
  ]
}
